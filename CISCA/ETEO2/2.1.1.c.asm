; Este primer ejercicio consiste en analizar unos fragmentos de código CISCA 
; que acceden a elementos de un vector y decir lo que hacen. Debes expresarlo 
; en un lenguaje de alto nivel, como el C (sin usar notación de punteros), o 
; usando un pseudo-lenguaje, o explicando claramente lo que hace el código 
; y por qué.

; Tenemos almacenado en memoria, a partir de la dirección simbólica V, un 
; vector de 100 elementos, donde cada elemento es un número entero codificado
;  en complemento a 2 con 32 bits. Recordad que en la dirección simbólica V 
;  se encuentra almacenado el elemento V[0], en la dirección V+4 se encuentra
;   V[1], etc. También tenemos almacenado en memoria, en la dirección 
;   simbólica A, un número entero codificado en complemento a 2 con 32 bits.

; Suponed que en el programa escrito en el lenguaje de alto nivel se ha definido
; la variable V como vector de enteros de 32 bits y la variable A como número 
; entero de 32 bits.
